services:
  - type: web
    name: yiyu-tts
    env: python
    rootDir: tts_server
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    plan: free
